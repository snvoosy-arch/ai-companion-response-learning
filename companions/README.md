# Companions

Canonical companion entry points:

- `../examples/sapphirus_external_first`: Sapphirus current CPU reference slice
- `rubrum`: Rubrum 판단·상태 중심 공개 포트폴리오

Historical implementation note:

- Sapphirus의 전체 초기 runtime snapshot은 main에서 제거하고
  `sapphirus-legacy-snapshot-v1` 태그에 보존
- 비공개 초기 실행 경로는 현재 Rubrum 공개 경로와 분리

Use the project README as the canonical map. The archived Sapphirus snapshot is
historical evidence, not the current runnable architecture.
